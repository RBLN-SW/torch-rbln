# Build/dev dependency constraints (single source of truth).
# TEMPORARY: a build of rebel_compiler#13235, which is not on rebel's dev branch yet.
# It is what declares rbln_exec_api.h and raises RBLN_ABI_CURRENT to 3, so the warm-cache
# hit path can be built against the C ABI instead of rebel's internal C++ symbols.
# Restore a production pin once 13235 ships in a release.
rebel-compiler==0.11.3.dev112+g9bb2161a
