# Test-only dependencies for model tests
# These packages are only needed when running model tests (test/models/*.py)
#
# This file intentionally lists only deps that are *not* transitively provided
# by vllm-rbln, to avoid version/index conflicts when both this loop and the
# vllm-rbln editable install try to manage the same package:
#   * torchvision — pinned to a specific CPU-index build (operator compat);
#     must come from download.pytorch.org/whl/cpu rather than PyPI.
#   * pandas — used by test/models/test_optimum_llm.py, not a vllm-rbln dep.
#
# The following are *expected* to arrive via vllm-rbln's own dependencies when
# ./tools/test/install-test-deps.sh runs the vllm-rbln section:
#   * transformers, optimum-rbln (both pinned by vllm-rbln's pyproject.toml).
# If VLLM_RBLN_SKIP=1 is used to opt out of vllm-rbln, tests that need
# transformers/optimum-rbln will fail to import — install them manually in
# that case.
#
# vllm + vllm-rbln themselves are handled entirely by install-test-deps.sh
# (search for VLLM_RBLN_REF); vllm-rbln's device-tensor branch is not on
# PyPI, so it is git-cloned + installed editable.
#
# Format: package==version
# Comments and empty lines are ignored
torchvision==0.25.0+cpu
pandas==2.2.3
